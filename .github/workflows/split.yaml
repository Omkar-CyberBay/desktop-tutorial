name: Duplicate Issue2

on:
  issues:
    types: [closed]

jobs:
  duplicate_issue:
    if: contains(github.event.issue.labels.*.name, 'POC') && github.event.action == 'closed' && github.event.issue.state_reason == 'completed'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Authenticate with GitHub CLI
        run: echo "${{ secrets.GH_TOKEN }}" | gh auth login --with-token

      - name: Duplicate the issue
        env:
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
          ASSIGNEE: 'Omkar-Net-Mon'
        run: |
          TITLE=$(gh issue view "$ISSUE_NUMBER" --repo "$REPO" --json title -q '.title')
          BODY=$(gh issue view "$ISSUE_NUMBER" --repo "$REPO" --json body -q '.body')
          DUPLICATE_CHECK=$(gh issue list --repo "$REPO" --search "1.${TITLE}" --json number -q '.[] | .number')

          if [ -z "$DUPLICATE_CHECK" ]; then
            gh issue create --repo "$REPO" --title "2.$TITLE split" --body "$BODY" --assignee "$ASSIGNEE"
            echo "Duplicate issue created."
          else
            echo "Duplicate issue already exists. No action taken."
          fi
